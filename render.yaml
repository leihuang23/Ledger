# Zero-cost Render Blueprint for the public read-only FastAPI API.
# Postgres (with pgvector) is provided by an external Supabase free project;
# the Blueprint manages only the API web service and the free Key Value
# instance. `DATABASE_URL` is a manually-set server-only secret pointing at the
# Supabase Supavisor session pool (port 5432); see docs/deployment.md.
# Async Celery execution is intentionally omitted because Render background
# workers have no free instance type and anonymous demo mutations are blocked.
# The Next.js app is deployed separately from apps/web on Cloudflare Workers.

services:
  - type: keyvalue
    name: ledger-redis
    plan: free
    ipAllowList: []
    maxmemoryPolicy: noeviction
    persistenceMode: off

  - type: web
    name: ledger-api
    runtime: docker
    plan: free
    dockerfilePath: ./apps/api/Dockerfile
    dockerContext: ./apps/api
    healthCheckPath: /ready
    autoDeployTrigger: checksPass
    envVars:
      - key: APP_ENV
        value: demo
      - key: APP_VERSION
        value: phase-6
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        fromService:
          name: ledger-redis
          type: keyvalue
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          name: ledger-redis
          type: keyvalue
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          name: ledger-redis
          type: keyvalue
          property: connectionString
      - key: BACKEND_CORS_ORIGINS
        sync: false
      - key: ALLOW_UNSAFE_BOOTSTRAP_SEED
        value: "true"
      - key: DEMO_OPERATOR_TOKEN
        generateValue: true
      - key: EVAL_RUN_TOKEN
        generateValue: true
      - key: DOCUMENT_INGEST_TOKEN
        generateValue: true
      - key: EMBEDDING_PROVIDER
        value: local
      - key: EMBEDDING_MODEL
        value: local-hashing-v1
      - key: LLM_PROVIDER
        value: none
      - key: OBSERVABILITY_PROVIDER
        value: local
      - key: OBSERVABILITY_FULL_PAYLOADS
        value: "false"
      - key: LOG_FORMAT
        value: json
      - key: RATE_LIMIT_MUTATIONS_PER_MINUTE
        value: "10"
      - key: RATE_LIMIT_SEARCH_PER_MINUTE
        value: "60"
